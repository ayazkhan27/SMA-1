# Figure 1c — five domains

> Assumes the **SHARED BRIEF** you already have (science context + design directives + style/palette). This file is ONLY this figure's content — do not re-derive the shared rules.

## What this panel must convey
**Breadth.** We test the *same* mechanism across **five unrelated, high-stakes
fields**, each grounded in its own real, expert-built golden ontology. The reader
should grasp: "this is a general principle, not a one-domain trick."

---

# FIGURE C — Five golden-ontology domains · artboard 1500 × 420

Title top-left: **"c  Tested across five domains".** A **horizontal row of five
shield badges** (rounded-top "shield" shape, ~230px tall, ~250px wide, evenly
spaced). Each shield: a **field icon** at top (flat single-colour line glyph in the
domain accent), the **domain name** bold in that accent, and below it the
**ontology + scale** (11px). A thin grey arrow enters each shield from a small
shared "domain router" pill centred above the row (echoing panel b).

1. **Medicine** — accent `#C0504D`, icon = medical cross / caduceus — "HPO · MONDO · GO · Uberon".
2. **Genomics** — accent `#3E8E5A`, icon = DNA double helix — "GO + GOA".
3. **Cyber** — accent `#4A6F8A`, icon = shield with keyhole — "ATT&CK · CAPEC · CWE".
4. **Legal / IP** — accent `#7A5DA8`, icon = balance scales — "CPC (254k nodes)".
5. **Finance** — accent `#C68A2E`, icon = coin / dollar — "US-GAAP (FIBO routed)".

(These are the actual ontologies used; the names can appear verbatim as labels.)
