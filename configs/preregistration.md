# SMA-1 Preregistration

Status: draft until git tag `prereg-v1`.

Primary hypotheses H1-H6 follow the blueprint. Calibration parameters are fit only on validation
fixtures and frozen as `score-v1` before test-set execution.

