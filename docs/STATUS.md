# SMA-1 Status Ledger

Append-only progress log. Each work session must reread
`structure_mapping_agentic_memory_blueprint.md` before changing implementation state.

## 2026-06-11

- Phase: P0 bootstrap.
- Gates attempted: none yet.
- Progress: repository scaffold started from the blueprint-only workspace.
- Next action: implement IR, matcher, encoders, retrieval, and gate tests.

## 2026-06-11

- Phase: P0-P6 MVP scaffold.
- Gates attempted: `make gates`.
- Gates passed: G0-G6 fixture gates, 8 tests passing.
- Metrics observed: fixture report generated by `make report`; Gradio UI served HTTP 200 at `http://127.0.0.1:7860/`.
- Progress: implemented deterministic IR/store, Tier-0 encoders, SME-style matcher, MAC/FAC retrieval, SAGE pools, FastAPI/CLI/Gradio entrypoints, dataset manifests, canonical fixtures, and report CSV schemas.
- Surprises: local environment exposes `python3` but not `python`; Makefile now uses `PYTHON ?= python3`.
- Remaining gaps: full external dataset downloads/checksum verification, SME v4 25-pair oracle execution, score calibration/freeze, and full blueprint section 8 paper evaluation.
- Next action: download selected datasets, convert LogHub/BugsInPy/ARN into cases, and replace fixture report rows with frozen evaluation outputs.

## 2026-06-11

- Phase: P4/P5 retrieval + UI correction.
- Gates attempted: `make gates`.
- Gates passed: G0-G6, 10 tests passing.
- Metrics observed: SSB forced-choice report now shows `r1=1.0000`, `mrr=1.0000`, `mapping_f1=1.0000`.
- Progress: fixed MAC/FAC candidate generation by canonicalizing content vectors and bounds; added a regression test for far analog screening; added an orchestration-only local LLM layer with optional quantized Qwen GGUF; replaced the Gradio tool debug UI with a toggleable SMA/RAG/knowledge-graph/context-only comparison workbench.
- Policy note: the LLM is downstream only. Extraction, SMA retrieval, RAG retrieval, and KG retrieval remain deterministic and are completed before evidence is passed to the model/verbalizer.
- Runtime: Gradio comparison UI served HTTP 200 at `http://127.0.0.1:7860/`.
- Remaining gaps: install/download the optional local model, add LangChain-backed non-fallback RAG/KG implementations when dependencies are installed, download real corpora, and run full blueprint section 8 evaluation.

## 2026-06-11 (evening session)

- Phase: P7 LogHub MVP diagnostic executed end-to-end; shallow-path remediation across matcher/index/eval/UI.
- Gates attempted: full `pytest tests/` (all gates). Passed: 11/11, including canonical battery after matcher changes.
- Matcher fixes (perf-critical, found via py-spy on a live run):
  - `Kernel.bindings` was a property rebuilt on every access (inside conflict-check inner loops, re-serializing statement trees each time) → now `cached_property`, plus cached `MatchHypothesis` keys and a `reverse_bindings` table; `kernels_conflict` iterates the smaller table.
  - MH seeding now groups by canonical functor signature and caps pairs per group (`MatchConfig.mh_group_cap=128`, U-ordered: identical statements first) — the §10.2 tripwire response for repeated event types.
  - Kernels are now built from root MHs only (§2.2); sub-matches live in their parents' closures.
  - Net effect: pathological 120-line session match went from >5 min to ~181 ms (~2000x); 265-statement match ≈50 ms, meeting the §2.8 target.
- Other shallow paths fixed: `MatchConfig.exact_kernel_limit`/`cpsat_time_ms` were ignored by `merge_cpsat` (now wired; silent ortools except removed); MAC/FAC early-stop compared a raw-score bound to the k-th raw score while ranking by ses_n (now bound/self_score(query) vs k-th ses_n; `certified` flag honest under fac_budget truncation); SSB generator reused one functor vocabulary across triples (cross-triple structural collisions, papered over by a histogram "structure_signature") → per-triple namespaces, workaround deleted; report.py pytest-sniffing → explicit `--skip-loghub`/`include_loghub`; `content_id` blake2b silent fallback removed (blake3 hard import); dead `baselines/kg_ppr.py` deleted; loghub_eval vote/record blocks deduplicated.
- UI: rebuilt Gradio workbench with toggleable modes (sma / bm25 / dense rag / knowledge graph / context only — mirrors eval baselines) and side-by-side answer cards with provenance + collapsible evidence; per-query Kuzu tempdir rebuild removed; BM25 uses real rank_bm25 scores; dense mode uses all-MiniLM-L6-v2 with explicit TF-IDF fallback. Served HTTP 200.
- LogHub MVP diagnostic results (1,000 sessions/dataset, 80/20 split, seed 42; no diagnostic alert rows):
  - HDFS: SMA macro-F1 0.9549 (hit@1 0.9550) vs BM25 0.8191, Dense RAG 0.6449, KG-PPR proxy 0.6625. SMA p50 346 ms.
  - BGL: SMA macro-F1 0.8739 (hit@1 0.8700) vs BM25 0.9800, Dense RAG 0.9950, KG-PPR proxy 0.9394. SMA p50 285 ms, p95 2.05 s.
- Honest reading: SMA dominates on HDFS (+13.6 F1 pts over best baseline) but trails on BGL, where per-message anomaly markers make surface retrieval near-perfect; H2 (within-domain non-inferiority, -2 pt margin) currently fails on BGL and must be reported as a finding. BGL p95 latency 2 s exceeds comfort; investigate large 60 s node windows.
- Surprises: first full run took >100 min before the matcher fixes; py-spy stack dump (run with sudo by the user) pinpointed `Kernel.bindings`. Output buffering hid all progress — `make report` now runs `python3 -u`.
- Note per §10.1: matcher scoring path changed (root-only kernels + MH cap) → mapping scores shifted on noisy cases; G2 canonical battery re-verified green. A score-v ADR entry is warranted before any calibration freeze.
- Next action: cross-system transfer eval (index BGL → query Thunderbird; HDFS → OpenStack) — the honest off-surface test (H1); investigate BGL gap (encoder rules for per-message markers vs structure); consider SES-vs-MDL ablation on the BGL split.

## 2026-06-11 (late session)

- BGL label-leak fix: pass-2 extraction was storing raw lines including the
  leading alert-category column (the ground-truth label). Now stripped
  (`line.partition(" ")[2]`). HDFS unaffected (labels live in a separate CSV).
- Corrected BGL numbers (HDFS reproduced bit-identically): SMA 0.8687 (was 0.8739),
  BM25 0.9750 (was 0.9800), Dense RAG 0.9950 (unchanged), KG-PPR 0.8895 (was 0.9394).
  Conclusion: the leak was real but minor — BGL anomalies are lexically overt in the
  message body itself, so surface methods legitimately dominate there. H2 finding stands.
- Thunderbird checksum mismatch confirmed in manifest; excluded until re-fetched.
- LLM layer: local Qwen now uses chat-format completion with repeat_penalty 1.25 /
  max_tokens 220 (fixes looping); verbalizer prompt carries numbered evidence text only
  (no provenance hashes — small models parrot them). Added DeepSeekOrchestrator
  (deepseek-chat via httpx; key from SMA_DEEPSEEK_API_KEY env or gitignored .env).
  UI gains an answer-model toggle (local vs DeepSeek) with per-card badges.
- UI: replaced gr.Dataframe (client-side render errors) with a controlled HTML corpus
  table; demo corpus pre-loads at startup; status chips; dark-mode-safe pinned colors.
- Gates: 11/11 pass. SMA BGL p95 latency 2.46 s remains the open tripwire item.

## 2026-06-11 (night session)

- Chat workbench: new Chat tab (gr.Chatbot) with per-turn retrieval, conversation
  history fed to the verbalizer (capped 8 turns), per-turn memory-mode/LLM/adapter/k
  toggles, and a live evidence side panel with label chips + label-vote line.
- Scale: scripts/prepare_ui_corpus.py extracts stratified labeled samples
  (5,000 HDFS + 2,500 BGL) to data/processed/*.jsonl; one-click loading in the UI
  with progress and deterministic shuffle (partial loads stay label-balanced).
  sma_evidence had an unbounded shortlist (15.3 s/query at 1k docs) → MAC/FAC
  budgets (shortlist 200, fac_budget 30): 1.0 s/query at 5,000 docs; encode 6 s.
- Challenge corpus + 5 validated hard incidents (gr.Examples in Chat and Compare);
  Q2/Q3/Q4 are sharp discriminators where BM25/dense/KG all retrieve the trap and
  SMA retrieves the true structural analog.
- logs encoder v0.1.1: cause/enables now require antecedent-before-consequent,
  matching rules/logs.yaml (Python previously ignored ordering). Encoder version
  bumped; LogHub eval numbers in this ledger predate v0.1.1 — re-run at next bench.
- Gates: 11/11 pass. Gradio 6 note: Chatbot no longer takes type=.

## 2026-06-11 (transfer + scorer ablation results)

- SES vs MDL ablation (ADR-004): SES wins within-system triage (HDFS 0.9549 vs
  0.8933; BGL tied 0.8687); MDL uniquely recovers rare failure families (EOF
  question: SES 0/5 EOF-family in top-5 even with verbatim log lines; MDL 3/5
  from prose). SES stays default; MDL exposed as UI scorer toggle; no IDF
  patching of SES (mandate). Cross-system runs executed under both scorers.
- Cross-system transfer (T2-b) FIRST RUN: NEGATIVE for H1 as run.
  HDFS->OpenStack: all four methods collapse (macro-F1 0.333, hit@1 ~0.50).
  BGL->Thunderbird(first20M): Dense RAG 0.7407, KG 0.6980, BM25 0.5489,
  SMA 0.3333 (all-Anomaly vote collapse; diagnostic alerts fired).
- Diagnostic decomposition (per blueprint section 12): cross-system canonical
  functor overlap = 4 functors (before/component/count/logSession) - 100/105
  HDFS functor types are content-hash template names, system-specific by
  construction. The matcher cannot map events the encoder names incomparably;
  this is the section 4.3 lattice/ontology gap, with the prescribed tripwire
  response "extend adapter ontology (data change)". Not yet evidence against
  structure mapping itself; encoder fix required before re-test.
- Also noted: SMA all-Anomaly collapse consistent with the short-session ses_n
  bias flagged earlier; revisit normalization at calibration time.

## 2026-06-11 (H3 mini-study, 200 cells: 20 questions x 5 modes x 2 LLMs, HDFS 5k corpus, k=5, SES)

- Abstention discipline is overwhelmingly a property of the VERBALIZER:
  DeepSeek abstained on 46/50 unanswerable cells (incl. 10/10 under sma and kg);
  the local Qwen2.5-0.5B abstained on 1/50 - it answers everything, i.e. near-
  total confabulation pressure on false premises. The window-boundary caveat is
  honored by a competent model and ignored by a 0.5B one.
- Second finding: DeepSeek also (correctly) declined most ANSWERABLE questions
  (answered 15/50; sma-SES worst at 1/10) because top-5 evidence under prose-only
  queries rarely contained the asked-for failure family - consistent with the
  ADR-004 rare-template result. Retrieval determines whether honesty has anything
  to work with; the verbalizer determines whether it is honest about it.
- Artifacts: reports/h3_mini_study.csv (answers + auto_abstained; human_correct /
  human_confabulated columns blank for review). Auto-detection is regex-based and
  conservative; human pass pending. Re-run variant under MDL scorer queued as
  follow-up (expects better answerable-question evidence).

## 2026-06-11 (cross-system transfer v2: ontology encoder)

- Logs encoder v0.2.0 added deterministic cross-system event classes
  (EVENT_CLASS_RULES: timeoutEvent/ioEvent/kernelEvent/networkEvent/
  storageEvent/lifecycleEvent/failureEvent/...), the section 4.3 tripwire
  response to the measured 4-functor cross-system overlap.
- BGL->Thunderbird(first20M)[ses] WITH ontology: SMA macro-F1 0.9093 (hit@1
  0.9100) vs Dense RAG 0.7407, BM25 0.5489, KG-PPR 0.3552. SMA went from
  vote-collapse 0.3333 (v1) to best-in-class +17 F1 pts over dense - the H1
  pattern: given a representation with comparable event vocabulary, structure
  transfers where surface does not. No diagnostic alerts for SMA in v2.
- HDFS->OpenStack remains all-methods collapse (~0.33 / coin-flip) under both
  encoders: OpenStack anomaly semantics (e.g. missing-cleanup) may be invisible
  to any retrieval formulation; investigate task design before re-attempting.
- Honesty caveats for the paper: single seed; ontology rules were written after
  observing the v1 failure (pre-freeze diagnostic, not a preregistered result);
  rules are generic ops vocabulary but partially informed by knowing the target
  domain family; needs held-out confirmation (new seeds/windows + a third pair)
  in the consolidated pre-freeze batch. MDL leg of v2 queued with that batch.

## 2026-06-12 (pre-freeze evidence batch, agent team)

- HELD-OUT TRANSFER CONFIRMED (frozen ontology-v1, Spirit from USENIX CFDR,
  downloaded after the freeze tag): BGL->Spirit seeds 42/7/19 SMA macro-F1
  0.9200/0.9650/0.9300 (mean 0.938) vs Dense RAG 0.3144/0.4137/0.3397 (mean
  0.356), BM25 ~0.39, KG ~0.36. MDL leg 0.9100 (consistent). The post-hoc
  ontology objection is answered: multi-seed, held-out, +58 F1 pts.
- HDFS->Spirit (cross-FAMILY transfer): SMA 0.3775 — fails, as HDFS->OpenStack
  did. Honest scope: structural transfer holds within the failure-physics
  family (supercomputer syslogs), not across app-vs-infra families. KG-PPR
  0.8648 here is anomalous and needs investigation before being believed.
- BASELINE LADDER (within-system, seed 42): HDFS — SMA 0.9549 still beats the
  production stack: Hybrid-RRF 0.8350, Hybrid+Rerank 0.7427, BGE 0.6377,
  SPLADE 0.4039, B6-LongContext-DeepSeek 0.8088. BGL — hybrid/BGE/SPLADE
  saturate (~1.0), confirming lexical overtness. CAVEAT: ladder latencies ran
  CPU-contended with the Spirit chain; do not cite them.
- WL-KERNEL CONTROL FINDING: generic WL graph similarity over SMA's own Tier-0
  cases scores 0.9799 on HDFS (> SMA 0.9549) at ~11ms. Within-system, cheap
  graph similarity on good extraction beats full SME. Decisive follow-up
  running now (scripts/transfer_controls.py): does WL / the hybrid stack
  transfer BGL->Spirit? Outcome determines whether the matcher's value claim
  is alignment or representation.
- FAMILY METRIC (Agent C): HDFS family-hit@1 SMA-ses 0.9057 vs BM25 0.6226,
  Dense 0.4906 — SMA finds the right failure family, not merely "an anomaly".
  BGL: Dense 0.9623 > SMA 0.68 (alert families are lexically marked). ADR-004
  REVISION NOTE: SES beats MDL on aggregate family-hit (HDFS 0.9057 vs
  0.8396); the EOF rare-family anecdote does not generalize. Rare-family-
  stratified breakdown queued before any scorer default change.
- H3 LLM-JUDGE (Agent D, all 200 cells, audit trail in reports/h3_judged.csv):
  DeepSeek 99% judged-correct, 0 invented entities, 2% confabulation (over-
  inference only); local Qwen-0.5B 2% correct, 18% confabulation, 0/100
  abstentions. Abstention regex validated: precision 0.96 / recall 0.87.
  Pipeline bug found and fixed: 400-char evidence cap truncated the anomaly
  lines questions ask about (now 900); some "abstentions" were artifacts.
- Reproducibility fix (Agent C finding): sampler remainder-fill iterated a
  Python set (hash-seed dependent across processes) -> sorted. Exact samples
  now process-independent.
- Ops lesson: a detached agent's leftover monitor double-launched the ladder
  run (killed; CSV unaffected). Policy: agents implement, the main session
  launches runs.

## 2026-06-12 (transfer controls: the matcher earns its keep)

- BGL->Spirit[seed42] controls: WL-kernel 0.6239, Hybrid+Rerank 0.5947,
  Hybrid-RRF 0.5171 — vs SMA 0.9200 on the identical index/query sets.
- Decomposition settled: the Tier-0 ontology representation is necessary
  (v1 collapse without it) but not sufficient (WL on the SAME representation
  trails SME by ~31 F1 pts cross-system); SME alignment is the active
  ingredient for transfer. Within-system the order inverts (WL 0.9799 >
  SME 0.9549 on HDFS at ~1ms) -> tiered retrieval design implication:
  WL prefilter within-system, SME for cross-system + provenance/inference.
- Production-RAG question closed: the deployed stack (hybrid+rerank) does
  not transfer (0.59 vs 0.94).

## 2026-06-12 (Phases 2-6 greenlit)

- User approved proceeding through all phases; Phase 1 declared complete.
- Phase 2 begins: surprisal-weighted trickle-down scorer ("surprisal") joins
  the codebase as score-v2 candidate - SES geometry with MDL corpus-surprisal
  weights (sigma_0 = -log2 p(functor) instead of 1; reduces exactly to SES
  when no corpus stats). Gauntlet: SES vs MDL vs RRF(SES,MDL) vs surprisal on
  family-hit (common + rare strata), the EOF micro-case, BGL->Spirit transfer,
  and the canonical battery. Winner becomes score-v2 by measurement, not fiat.

## 2026-06-12 (Phase 2 results: gauntlet parts A-B, BugsInPy T3)

- SCORER GAUNTLET (family-hit@5, 1000-session protocol): HDFS rare families
  (<=20 index exemplars, n=6 queries): surprisal 0.5333 > mdl 0.4167 >
  rrf 0.1833 > ses 0.1500 - the principled merger more than triples SES on
  rare families. HDFS common (n=100): ses 0.9000 vs surprisal 0.8860 (-1.4
  pts, negligible). BGL: all four variants tied (~0.66-0.67; families are
  lexical there). RRF (the cheap control) decisively beaten on rare - the
  one-scorer merger is justified over the combo.
- EOF micro-case now 0/5 for ALL scorers (was mdl 3/5 pre-encoder-v0.2.0):
  the ontology class statements added for transfer dilute rare-template
  surprisal in prose queries. Honest note: the EOF anecdote is confounded by
  the encoder change; the rare-strata result above is the systematic evidence.
  Encoder/scorer interaction logged as a calibration-time consideration.
- Short-session bias indicator: top-5 mean lines 16.2-16.3 vs corpus 18.2 on
  HDFS (mild, scorer-independent); BGL pulls LONG sessions (137-165 vs 65) -
  the earlier "short-session bias" concern is not scorer-driven and not
  systematic across datasets; documented, no normalization change needed
  before calibration.
- BUGSINPY T3 (Agent E, 500 bugs, 17 projects): cross-project (LOPO)
  fix-category-hit@1: SMA 0.3382 vs BM25 0.1863 vs Dense 0.1127 (random floor
  0.094). Structure transfers fix patterns across codebases where text does
  not. Caveats recorded: taxonomy covers 41% of bugs ("other" 59% excluded
  per predefined rule); ~90ms vs 5ms latency. Second domain (code) now
  demonstrated end-to-end.
- transfer_eval --scorer now accepts "surprisal"; BGL->Spirit surprisal leg
  re-running (was blocked by argparse choices).

## 2026-06-12 (score-v2 decided: surprisal-SES, ADR-005)

- Transfer leg completed the gauntlet sweep: BGL->Spirit[seed42] surprisal
  0.9300 (> ses 0.9200, mdl 0.9100), p50 279ms (< ses 577ms; weighted bound
  tightens early-stop). Surprisal wins/ties every gauntlet column; only loss
  is -1.4pts on HDFS common families.
- Default scorer is now "surprisal" (MatchConfig); UI toggle offers
  surprisal/ses/mdl; set_scorer accepts all three. ADR-005 records the
  decision table. 15/15 tests green (incl. new BugsInPy unit tests).
- Phase 2 of the ledger is COMPLETE (scorer resolution + bias study + T3).
  Next: Phase 3 freeze train - SSB de-circularization, calibration on
  validation splits, prereg tag, statistics runs.

## 2026-06-12 (production loop: coverage indicator, draft adapter, fused mode)

- Coverage indicator (blueprint 12-R3): per-query structural-coverage chip in
  the UI; below 40% the verbalizer receives an explicit low-confidence caveat.
  Motivated by a live test where an invented-vocabulary incident degenerated
  SMA retrieval to shape-matching silently.
- LLM-drafted adapters (rules-not-facts discipline): DraftAdapter composes the
  frozen base encoder with extra keyword classes supplied as a content-
  addressed, determinism-checked JSON artifact; cases/receipts carry
  "draft-adapter (LLM-proposed, unreviewed) hash=..." forever. Real DeepSeek
  draft for the watcher/grain-drift system produced exactly two minimal
  classes (grainDriftEvent, fenceStateEvent). Guards: residual-only drafting
  (LLM sees only lines that fire no frozen rule) + keyword-level dedup against
  the frozen ontology (redundant rules double-count and dilute surprisal -
  the measured EOF regression is the precedent). Agent-found trap pinned by
  regression test: LLM-proposed maskings can erase the substrings their own
  class keywords need; maskings are stored but not applied before matching.
- Hybrid (fused) mode: RRF over bm25+dense+sma top-20s, with SME alignment
  receipts computed for every fused candidate - retrieval as a committee,
  accountability via alignment regardless of which retriever found the case.
- 30/30 tests; frozen paths untouched (logs_drain.py, sma/match, sma/index).

## 2026-06-12/13 (Liberty needle-in-haystack: a measured solo-SMA failure)

- New held-out corpus: Liberty (CFDR, md5-verified), needle-in-haystack UI
  sample: 5,000 sessions @ 5.0% admin-alert needles (pbs_mom scheduler
  failures), sampled from the alert-storm transition region (lines 30M-60M;
  first 40M lines contain almost no alerts - recon documented).
- Probe (held-out pbs_mom needle, new node): BM25 5/5, Dense 5/5, hybrid 3/5,
  SMA 0/5. Diagnosis (two compounding stages): (1) ANN cosine prescreen keeps
  only 1/250 needles in top-200 while the certified BOUND ordering puts 21
  needles in its top-30 - the MAC shortlist-recall tripwire (blueprint 10.2)
  firing on burst-shaped sessions; (2) scoring favors tiny repeated-line
  normal sessions (needle ses_n 0.06-0.08 vs 0.77), and min-normalization
  (10.2 tripwire option, now implemented as MatchConfig.normalization,
  default unchanged "max") produced ses_n>1 on a cross-match - an arithmetic
  anomaly in the trickle-down x surprisal x burst interaction requiring a
  focused investigation. BOTH queued as Phase 3 work items (MAC recall fix;
  scoring forensics) - pre-freeze, exactly where they belong.
- Production posture meanwhile: hybrid (fused) is the recommended haystack
  mode - lexical/dense candidates + SMA alignment receipts (by design:
  retrieval committee, accountability layer).
- Draft-adapter robustness finding: Liberty needs NO draft adapter - frozen
  ontology covers stock Linux syslog at ~100% median coverage. The recursion
  terminates when the rulebook already speaks the dialect; drafting is for
  genuinely alien vocabularies (the watcher demo).

## 2026-06-13 (Phase 3 forensics, part 1: constants constraint + bound-ordered MAC)

- ROOT CAUSE 1 FIXED (vacuous scaffolding): constant-like entities
  (event_type template names, integers) now match only identically
  (CONSTANT_ENTITY_TYPES in mh.py; kernels pairing unequal constants are
  discarded). Effect measured on Liberty: vacuous tiny-normal raw scores
  101.7 -> 13.2; needle raw scores now BEAT small normals (23.5 vs 13.2).
  Blueprint 2.1 entities-vs-constants discipline, previously unimplemented.
- ROOT CAUSE 2 FIXED (MAC recall): retrieval now bound-orders ALL candidates
  via the weighted Lemma-2 inverted index for corpora <= 20k cases (ANN
  cosine pre-screen demoted to a >20k scale optimization; it was keeping
  1/250 needles in top-200 while bound ordering had 21 in its top-30).
  Zero-overlap candidate padding keeps brute-force parity (gate G4 green).
- STILL OPEN: haystack top-5 remains 0/5 because max-normalization divides
  needle scores by their own 60-line bulk (raw ordering is now correct;
  ses_n ordering inverts it), and min-normalization exhibits ses_n > 1 on
  cross-matches (score exceeding self-score) - a trickle-down arithmetic
  anomaly under forensic investigation. Until resolved, normalization
  default stays "max" and the haystack failure stands as documented.
- DISCIPLINE: these are matcher-semantics changes. ALL previously reported
  numbers (HDFS 0.9549, family-hit, BGL->Spirit 0.93/0.92) are UNCITABLE
  until the verification battery re-runs under the new semantics. Gauntlet +
  transfer leg launched; results land in the next ledger entry.
